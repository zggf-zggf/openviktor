{
	"name": "@openviktor/shared",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"pino": "^9.6.0",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"pino-pretty": "^13.1.3",
		"typescript": "^5.8.0",
		"vitest": "^3.0.0"
	}
}
